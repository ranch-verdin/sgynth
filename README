This old repo is full of old, probably dead hacks.  The part I'm keen
to share is a norns DSP engine host.

to build:

./build_all.sh

then:
./engine

then in matron:

-- simple C thing built from engine_example.c
load_engine('./engine_example.so')
engine.prang(0.5)
engine.prong(0.2)

-- assorted faust things...
load_engine('./clack.so')
engine.cp_gate(1.0)
engine.cp_decay(200.0)
engine.cp_gate(1.0)

load_engine('./snarf.so')
engine.sd_sd(1.0)

load_engine('./drumbum.so')
engine.bd_bd(1.0)

load_engine('./hatz.so')
engine.hatz_hh(1.0)
engine.hatz_oh(1.0)

load_engine('./string.so')
engine.thing_string1_freq(200.0)
engine.thing_string2_freq(300.0)
engine.thing_string3_freq(250.0)
engine.thing_string1_gate(1.0);engine.thing_string2_gate(1.0); engine.thing_string3_gate(1.0);